Tool for automating backing up and hasing websites (wget and hashdeep), developed for backing up EPA sites and data. Takes CSV files as input -- example input files are in /input

Usage: 
    downloader.py -i <input csv>
